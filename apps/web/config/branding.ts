// The only place in the web app that knows what the product is called.
// The codename is disposable; nothing else may hardcode it.

export const branding = {
  productName: "Bureau",
  description: "Autonomous QA platform",
} as const;
